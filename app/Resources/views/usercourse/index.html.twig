{% extends 'base.html.twig' %}

{% block body %}
    <h1>UserCourse list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for userCourse in userCourses %}
            <tr>
                <td><a href="{{ path('usercourse_show', { 'id': userCourse.id }) }}">{{ userCourse.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('usercourse_show', { 'id': userCourse.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('usercourse_edit', { 'id': userCourse.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('usercourse_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
