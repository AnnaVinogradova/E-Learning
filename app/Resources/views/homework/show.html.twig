{% extends 'base.html.twig' %}

{% block body %}
    <h1>Homework</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ homework.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ homework.content }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ homework.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('homework_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('homework_edit', { 'id': homework.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
