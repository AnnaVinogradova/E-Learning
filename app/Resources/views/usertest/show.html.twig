{% extends 'base.html.twig' %}

{% block body %}
    {{ current.text }}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" />
    {{ form_end(form) }}
{% endblock %}
